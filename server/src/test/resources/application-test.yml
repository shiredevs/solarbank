server:
  ssl:
    key-store: classpath:test-keystore.p12
    key-store-password: changeit

application:
  cors:
    allowed-origin: https://localhost:3030
  endpoint:
    nasa-power-base-url: http://localhost:${wiremock.port}

wiremock:
  port: 9999